const distinct = (dataset, rows) => [...new Map(rows.map((row) => [String(row.mode || '').trim().toUpperCase(), { dataset, mode: String(row.mode || '').trim(), description: String(row.description || '').trim() }]).filter(([key]) => key)).values()];
export async function refreshDefectModeStaging({ taSource, scSource, target }) { const [ta, sc] = await Promise.all([taSource.getDefectModes(), scSource.getDefectModes()]); if (!ta.length || !sc.length) throw new Error('MES returned no defect modes.'); await Promise.all([target.addModes('TA', distinct('TA', ta).map(({ mode, description }) => ({ mode, description }))), target.addModes('SC', distinct('SC', sc).map(({ mode, description }) => ({ mode, description })))]); return { ta: ta.length, sc: sc.length }; }
